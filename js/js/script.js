// let 変数定義
let name ="name";

// 再定義時はlet無し
name = "new name";

// console.log 出力
console.log(name);

// const (const 定数名 = "値") 定数定義
const birthyear = 2006;
console.log(birthyear);

// if, slse if, else 条件分岐
// &&, || 論理演算子
// switch 条件分岐  case 条件式 break; default: デフォルトの処理 break;
// for (let 変数定義; 条件式; 変数の更新(++(+1) --(-1))) {処理｝)
// `${}` 文字列の中で変数を展開